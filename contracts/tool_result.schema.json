{
  "$defs": {
    "ErrorCategory": {
      "enum": [
        "validation_error",
        "resource_not_found",
        "timeout",
        "external_service_error",
        "permission_denied",
        "conflict",
        "business_error",
        "internal_error"
      ],
      "title": "ErrorCategory",
      "type": "string"
    },
    "JsonValue": {},
    "ToolError": {
      "additionalProperties": false,
      "properties": {
        "category": {
          "$ref": "#/$defs/ErrorCategory"
        },
        "code": {
          "title": "Code",
          "type": "string"
        },
        "message": {
          "title": "Message",
          "type": "string"
        },
        "retryable": {
          "title": "Retryable",
          "type": "boolean"
        },
        "details": {
          "anyOf": [
            {
              "additionalProperties": {
                "$ref": "#/$defs/JsonValue"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Details"
        }
      },
      "required": [
        "category",
        "code",
        "message",
        "retryable"
      ],
      "title": "ToolError",
      "type": "object"
    },
    "ToolStatus": {
      "enum": [
        "ok",
        "retryable_error",
        "fatal_error"
      ],
      "title": "ToolStatus",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Success data is strict Output Model-validated, JSON-exported provider data.\n\nThis envelope checks JSON validity, not the tool-specific output schema.",
  "properties": {
    "status": {
      "$ref": "#/$defs/ToolStatus"
    },
    "data": {
      "$ref": "#/$defs/JsonValue",
      "default": null
    },
    "error": {
      "anyOf": [
        {
          "$ref": "#/$defs/ToolError"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "metadata": {
      "additionalProperties": {
        "$ref": "#/$defs/JsonValue"
      },
      "title": "Metadata",
      "type": "object"
    }
  },
  "required": [
    "status"
  ],
  "title": "ToolResult",
  "type": "object"
}
